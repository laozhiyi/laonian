# 橘上生香后端接口文档

## 基础信息

| 项目 | 值 |
|------|-----|
| 基础URL | `https://cslsgwpuqrsi.sealoshzh.site/v1` |
| 认证方式 | Bearer Token (JWT) |
| 数据格式 | JSON |
| 编码 | UTF-8 |

---

## 通用说明

### 请求头
```
Content-Type: application/json
Authorization: Bearer {token}
```

### 成功响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

### 错误响应格式
```json
{
  "code": 400,
  "message": "错误信息"
}
```

### 状态码说明
| 状态码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权，需要登录 |
| 403 | 无权限访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

---

## 一、用户模块

### 1.1 用户注册

**接口**: `POST /auth/register`

**请求参数**:
```json
{
  "username": "string",    // 用户名，4-20位，必填
  "password": "string",    // 密码，6-20位，必填
  "role": "user"           // 角色：user/admin，默认user
}
```

**成功响应**:
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "id": "5f8d0d55b54764421b7156c9",
    "username": "test",
    "role": "user",
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

**失败响应**:
```json
// 用户名已存在
{
  "code": 400,
  "message": "用户名已存在"
}

// 用户名格式错误
{
  "code": 400,
  "message": "用户名需为4-20位"
}

// 密码格式错误
{
  "code": 400,
  "message": "密码需为6-20位"
}
```

---

### 1.2 用户登录

**接口**: `POST /auth/login`

**请求参数**:
```json
{
  "username": "string",    // 必填
  "password": "string"     // 必填
}
```

**成功响应**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "id": "5f8d0d55b54764421b7156c9",
    "username": "test",
    "avatar": "",
    "role": "user",
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

**失败响应**:
```json
{
  "code": 400,
  "message": "用户名或密码错误"
}
```

---

### 1.3 用户登出

**接口**: `POST /auth/logout`

**请求头**: `Authorization: Bearer {token}`

**成功响应**:
```json
{
  "code": 200,
  "message": "登出成功"
}
```

---

### 1.4 获取当前用户信息

**接口**: `GET /user/profile`

**请求头**: `Authorization: Bearer {token}`

**成功响应**:
```json
{
  "code": 200,
  "data": {
    "id": "5f8d0d55b54764421b7156c9",
    "username": "test",
    "avatar": "https://example.com/avatar.jpg",
    "role": "user",
    "createdAt": "2024-01-24T10:00:00.000Z"
  }
}
```

**失败响应**:
```json
{
  "code": 401,
  "message": "未授权，需要登录"
}
```

---

### 1.5 修改用户信息

**接口**: `PUT /user/profile`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "avatar": "https://example.com/new-avatar.jpg"  // 头像URL
}
```

**成功响应**:
```json
{
  "code": 200,
  "data": {
    "id": "5f8d0d55b54764421b7156c9",
    "username": "test",
    "avatar": "https://example.com/new-avatar.jpg",
    "role": "user",
    "createdAt": "2024-01-24T10:00:00.000Z"
  }
}
```

---

## 二、商品模块

### 2.1 获取商品列表

**接口**: `GET /products`

**请求参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | int | 否 | 页码，默认1 |
| size | int | 否 | 每页数量，默认10 |
| keyword | string | 否 | 搜索关键词 |

**请求示例**:
```
GET /products?page=1&size=10&keyword=苹果
```

**成功响应**:
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "_id": "5f8d0d55b54764421b7156c9",
        "title": "红富士苹果",
        "cover": "https://example.com/apple.jpg",
        "priceNow": 59.90,
        "priceOrigin": 79.90,
        "tags": ["助农产品", "当季鲜果"],
        "description": "新鲜红富士苹果",
        "salesCount": 100,
        "createdAt": "2024-01-24T10:00:00.000Z",
        "updatedAt": "2024-01-24T10:00:00.000Z"
      }
    ],
    "total": 50,
    "page": 1,
    "size": 10
  }
}
```

---

### 2.2 获取商品详情

**接口**: `GET /products/:id`

**请求示例**:
```
GET /products/5f8d0d55b54764421b7156c9
```

**成功响应**:
```json
{
  "code": 200,
  "data": {
    "_id": "5f8d0d55b54764421b7156c9",
    "title": "红富士苹果",
    "cover": "https://example.com/apple.jpg",
    "images": [
      "https://example.com/apple1.jpg",
      "https://example.com/apple2.jpg"
    ],
    "priceNow": 59.90,
    "priceOrigin": 79.90,
    "tags": ["助农产品", "当季鲜果"],
    "description": "新鲜红富士苹果，产自优质果园",
    "stock": 100,
    "salesCount": 100,
    "status": 1,
    "createdAt": "2024-01-24T10:00:00.000Z",
    "updatedAt": "2024-01-24T10:00:00.000Z"
  }
}
```

**失败响应**:
```json
{
  "code": 404,
  "message": "商品不存在"
}
```

---

### 2.3 创建商品（管理员）

**接口**: `POST /products`

**请求头**: `Authorization: Bearer {token}` (需admin角色)

**请求参数**:
```json
{
  "title": "红富士苹果",           // 商品标题，必填
  "cover": "https://example.com/apple.jpg",  // 封面图片URL，必填
  "images": [                    // 商品图片列表
    "https://example.com/apple1.jpg"
  ],
  "priceNow": 59.90,             // 现价，必填
  "priceOrigin": 79.90,          // 原价
  "tags": ["助农产品"],           // 标签
  "description": "新鲜红富士苹果", // 商品描述
  "stock": 100                   // 库存
}
```

**成功响应**:
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": "5f8d0d55b54764421b7156c9"
  }
}
```

**失败响应**:
```json
// 非管理员
{
  "code": 403,
  "message": "无权限访问"
}

// 缺少必填字段
{
  "code": 400,
  "message": "缺少必填字段"
}
```

---

### 2.4 更新商品（管理员）

**接口**: `PUT /products/:id`

**请求头**: `Authorization: Bearer {token}` (需admin角色)

**请求参数** (可部分更新):
```json
{
  "title": "红富士苹果（精品）",
  "priceNow": 69.90,
  "stock": 50
}
```

**成功响应**:
```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "_id": "5f8d0d55b54764421b7156c9",
    "title": "红富士苹果（精品）",
    "priceNow": 69.90,
    ...
  }
}
```

---

### 2.5 删除商品（管理员）

**接口**: `DELETE /products/:id`

**请求头**: `Authorization: Bearer {token}` (需admin角色)

**成功响应**:
```json
{
  "code": 200,
  "message": "删除成功"
}
```

---

## 三、购物车模块

### 3.1 获取购物车列表

**接口**: `GET /cart`

**请求头**: `Authorization: Bearer {token}`

**成功响应**:
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": "5f8d0d55b54764421b7156c9",
        "productId": "5f8d0d55b54764421b7156c8",
        "title": "红富士苹果",
        "cover": "https://example.com/apple.jpg",
        "price": 59.90,
        "quantity": 2,
        "stock": 100
      }
    ],
    "totalCount": 2,
    "totalPrice": 119.80
  }
}
```

---

### 3.2 添加商品到购物车

**接口**: `POST /cart/items`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "productId": "5f8d0d55b54764421b7156c8",  // 商品ID，必填
  "quantity": 1                             // 数量，默认1
}
```

**成功响应**:
```json
{
  "code": 200,
  "message": "添加成功"
}
```

**失败响应**:
```json
// 商品不存在
{
  "code": 404,
  "message": "商品不存在"
}
```

---

### 3.3 修改购物车商品数量

**接口**: `PUT /cart/items/:id`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "quantity": 3  // 数量，大于0
}
```

**成功响应**:
```json
{
  "code": 200,
  "message": "更新成功"
}
```

**失败响应**:
```json
// 数量为0时自动删除
{
  "code": 200,
  "message": "删除成功"
}

// 购物车商品不存在
{
  "code": 404,
  "message": "购物车商品不存在"
}
```

---

### 3.4 删除购物车商品

**接口**: `DELETE /cart/items/:id`

**请求头**: `Authorization: Bearer {token}`

**成功响应**:
```json
{
  "code": 200,
  "message": "删除成功"
}
```

---

### 3.5 清空购物车

**接口**: `DELETE /cart`

**请求头**: `Authorization: Bearer {token}`

**成功响应**:
```json
{
  "code": 200,
  "message": "清空成功"
}
```

---

## 四、订单模块

### 4.1 创建订单

**接口**: `POST /orders`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
```json
{
  "cartItemIds": ["5f8d0d55b54764421b7156c9"],  // 购物车商品ID数组，必填
  "addressId": "string",                        // 收货地址ID（预留）
  "remark": "请尽快发货"                         // 订单备注
}
```

**成功响应**:
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "orderId": "5f8d0d55b54764421b7156c7",
    "orderNo": "ORD123456789ABC",
    "totalPrice": 119.80
  }
}
```

**失败响应**:
```json
// 购物车为空
{
  "code": 400,
  "message": "购物车商品不能为空"
}

// 库存不足
{
  "code": 400,
  "message": "商品 红富士苹果 库存不足"
}
```

---

### 4.2 获取订单列表

**接口**: `GET /orders`

**请求头**: `Authorization: Bearer {token}`

**请求参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| status | string | 否 | 订单状态：pending/paid/shipped/completed/cancelled |
| page | int | 否 | 页码，默认1 |
| size | int | 否 | 每页数量，默认10 |

**请求示例**:
```
GET /orders?status=pending&page=1&size=10
```

**成功响应**:
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "_id": "5f8d0d55b54764421b7156c7",
        "orderNo": "ORD123456789ABC",
        "status": "pending",
        "totalPrice": 119.80,
        "items": [
          {
            "title": "红富士苹果",
            "cover": "https://example.com/apple.jpg",
            "price": 59.90,
            "quantity": 2
          }
        ],
        "createdAt": "2024-01-24T10:00:00.000Z"
      }
    ],
    "total": 10
  }
}
```

---

### 4.3 获取订单详情

**接口**: `GET /orders/:id`

**请求头**: `Authorization: Bearer {token}`

**成功响应**:
```json
{
  "code": 200,
  "data": {
    "_id": "5f8d0d55b54764421b7156c7",
    "orderNo": "ORD123456789ABC",
    "status": "pending",
    "totalPrice": 119.80,
    "address": {
      "name": "张三",
      "phone": "13800138000",
      "detail": "北京市朝阳区xxx街道xxx号"
    },
    "items": [
      {
        "title": "红富士苹果",
        "cover": "https://example.com/apple.jpg",
        "price": 59.90,
        "quantity": 2
      }
    ],
    "remark": "请尽快发货",
    "createdAt": "2024-01-24T10:00:00.000Z"
  }
}
```

**失败响应**:
```json
{
  "code": 404,
  "message": "订单不存在"
}
```

---

## 五、接口测试示例

### 5.1 注册用户
```bash
curl -X POST https://cslsgwpuqrsi.sealoshzh.site/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"123456"}'
```

### 5.2 登录
```bash
curl -X POST https://cslsgwpuqrsi.sealoshzh.site/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"123456"}'
```

### 5.3 获取商品列表
```bash
curl -X GET "https://cslsgwpuqrsi.sealoshzh.site/v1/products?page=1&size=10" \
  -H "Content-Type: application/json"
```

### 5.4 获取用户信息
```bash
curl -X GET https://cslsgwpuqrsi.sealoshzh.site/v1/user/profile \
  -H "Authorization: Bearer {token}"
```

---

> 文档版本: v1.0
> 更新日期: 2026-01-24
